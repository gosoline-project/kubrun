apiVersion: v1
kind: Service
metadata:
  name: kubrun
  namespace: kubrun
  labels:
    app: kubrun
spec:
  selector:
    app: kubrun
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  type: ClusterIP
