apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubrun
  namespace: kubrun
  labels:
    app: kubrun
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kubrun
  template:
    metadata:
      annotations:
        autoscaling.cast.ai/removal-disabled: "true"
      labels:
        app: kubrun
    spec:
      serviceAccountName: kubrun
      nodeSelector: {{ .Values.kubrun.nodeSelector | toYaml | nindent 8 }}
      tolerations: {{ .Values.kubrun.tolerations | toYaml | nindent 8 }}
      containers:
        - name: kubrun
          image: ghcr.io/gosoline-project/kubrun:main-684a84e
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8890
              name: http
          args: ["--config=config.yml"]
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          volumeMounts:
            - name: kubrun-config
              mountPath: /app/config.yml
              subPath: config.yml
      volumes:
        - name: kubrun-config
          configMap:
            name: kubrun-config
            items:
              - key: config.yml
                path: config.yml